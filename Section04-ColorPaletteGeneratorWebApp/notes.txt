# Create new virtual environment:
$ python -m venv env
$ source env/bin/activate
$source env/Scripts/activate (Windows/Git Bash)

# Install Flask:
$ pip install Flask

# Run Flask:
$ flask run
$ flask run --debug